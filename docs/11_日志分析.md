# 脱敏日志分析

## 权威语料计数

以下计数来自 2026-08-12 的只读核验，与 [`RESEARCH_PROVENANCE.md`](RESEARCH_PROVENANCE.md) 使用相同口径：按文本行统计，不包含文件尾空行推断。

| 私有源文件 | 字节数 | 行数 | 公开情况 |
|------------|--------|------|----------|
| `SteamPatchToast_patch.lo` | 115,636 | 2,595 | 不发布原文 |
| `SteamPatchToast_startup.lo` | 855 | 18 | 不发布原文 |
| `_downloadlog/[REDACTED_STEAMID64].txt` | 9,963 | 104 | 不发布原文 |

总计 2,717 行。仓库仅提供 11 行人工筛选、脱敏夹具，不能据此重建完整源日志。

## 历史事件摘要

脱敏记录支持以下时间顺序：

1. 2026-06-15 23:25 左右，日志记录了启动、Steam 路径发现和账户相关配置读取；
2. 随后出现进程终止、UI 资源修改、异常 URL 构建和禁用更新配置；
3. 守护检查在历史记录中重复出现；
4. 心跳语料从 2026-06-15 23:25:44 延续至 2026-06-16 22:53:44。

Steam 安装路径和所有账户标识均从公开材料删除。历史主机写作 `nexustechsolution[.]top`，项目不检查其当前状态。

## 证据与推断

| 结论 | 证据等级 | 限制 |
|------|----------|------|
| 客户端文件发生修改 | 直接日志观察 | 尚需可信版本做文件完整性对比 |
| 心跳记录持续约 23.5 小时 | 直接日志观察 | 不证明服务端接收或当前在线 |
| 守护逻辑定期检查补丁 | 直接日志观察 | 间隔存在变化，不应写成绝对固定值 |
| 组件在该窗口持续运行 | 防御性推断 | 缺少完整进程遥测 |
| 终端已彻底清理 | 未验证 | 公开项目无法访问原主机 |

## 公开夹具

`backend/src/main/resources/samples/` 中的 11 行文本只覆盖解析器所需的代表性格式。它们已经删除用户、路径和可点击基础设施，并会在入库前再次经过 `ThreatIntelSanitizer`。

不要将夹具计数、数据库种子条目或前端统计值表述为原始语料规模。
