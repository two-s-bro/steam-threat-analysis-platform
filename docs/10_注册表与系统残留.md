# 注册表与系统残留 — 事件证据摘要

> 本页记录单次事件中直接观察到或调查过的信号。状态只描述当时的受控清理记录，不代表其他主机，也不提供自动删除命令。

## 已观察的用户级持久化

调查位置：`HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`

| 键名 | 关联文件名 | 事件记录状态 |
|------|------------|--------------|
| `SteamHelper` | `lizercllaxe.exe` | 已从受影响主机移除 |
| `SteamService` | `snapshot.exe` | 已从受影响主机移除 |
| `WindowsUpdateService` | `gameSatorHost.exe` | 已从受影响主机移除 |

这些名称可能与正常软件发生冲突。调查时应同时核验完整路径、签名、哈希、父进程和首次出现时间。

## 已观察的目录和文件信号

| 信号 | 防御用途 |
|------|----------|
| `%LocalAppData%\Programs\01anu7963sndw1ua\` | 与首阶段组件聚类核验 |
| `%LocalAppData%\ServiceApp\` | 与主控和注入组件聚类核验 |
| `%AppData%\v6vslsk8sfjeiqau\` | 与用户级 Run 键关联核验 |
| `%LocalAppData%\Temp\launcher_*\` | 临时落地目录调查 |
| `merged_steam.py` | 结合哈希与创建来源调查，不能仅凭文件名判恶 |

## 客户端篡改信号

| 文件 | 观察到的变化 | 建议核验 |
|------|--------------|----------|
| `steamui/chunk~2dcc5aaf7.js` | 帮助路由附近出现异常跳转逻辑 | 与可信版本做哈希/差异比较 |
| `steam.cfg` | 同时出现两项禁用更新配置 | 核验创建者、时间和组织策略 |

## 当时未确认的机制

调查记录没有确认 hosts、DNS 缓存、Winlogon、计划任务、WMI 订阅或 Windows 服务层面的额外持久化。这里的“未确认”不等于不存在；它只限定当前证据支持的结论。

安全处置步骤见 [`how-to-remove.md`](how-to-remove.md)。不要在未核验路径归属和证据需求前删除注册表项或目录。
