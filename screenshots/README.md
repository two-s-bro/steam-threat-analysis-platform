# Put PNG screenshots here — open http://localhost:5173 and press Win+Shift+S on each page

# Required screenshots:
#   dashboard.png   — Main dashboard with stats + C2 panel + attack chain
#   iocs.png        — IOC list with search/filter
#   timeline.png    — Attack timeline with phase filter
#   components.png  — Component force-directed graph
#   yara.png        — YARA rules list + match test
#   logs.png        — Log viewer with real malware logs imported
